import ToolClient from './ToolClient';

export const metadata = { title: 'Protect PDF', description: 'Add password protection to your PDF to prevent unauthorized access.' };

export default function Page() {
  return <ToolClient />;
}
