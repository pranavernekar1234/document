import ToolClient from './ToolClient';

export const metadata = { title: 'PDF to PDF/A', description: 'Convert PDFs to the archival PDF/A format for long-term preservation.' };

export default function Page() {
  return <ToolClient />;
}
