import ToolClient from './ToolClient';

export const metadata = { title: 'Sign PDF', description: 'Add digital signatures to your PDF documents securely.' };

export default function Page() {
  return <ToolClient />;
}
