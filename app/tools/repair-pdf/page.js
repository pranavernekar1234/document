import ToolClient from './ToolClient';

export const metadata = { title: 'Repair PDF', description: 'Fix corrupted or damaged PDF files and recover lost content.' };

export default function Page() {
  return <ToolClient />;
}
