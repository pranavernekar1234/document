import ToolClient from './ToolClient';

export const metadata = { title: 'Translate PDF', description: 'Translate PDF documents to 100+ languages while preserving formatting.' };

export default function Page() {
  return <ToolClient />;
}
