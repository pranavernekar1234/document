import ToolClient from './ToolClient';

export const metadata = { title: 'HTML to PDF', description: 'Convert HTML files to PDF documents with CSS styling preserved.' };

export default function Page() {
  return <ToolClient />;
}
