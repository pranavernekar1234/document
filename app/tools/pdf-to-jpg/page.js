import ToolClient from './ToolClient';

export const metadata = { title: 'PDF to JPG', description: 'Convert PDF pages to high-quality JPG images — one image per page.' };

export default function Page() {
  return <ToolClient />;
}
