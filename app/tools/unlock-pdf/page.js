import ToolClient from './ToolClient';

export const metadata = { title: 'Unlock PDF', description: 'Remove password protection from a PDF you have access to.' };

export default function Page() {
  return <ToolClient />;
}
