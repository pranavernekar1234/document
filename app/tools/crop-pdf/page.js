import ToolClient from './ToolClient';

export const metadata = { title: 'Crop PDF', description: 'Crop the margins and visible area of your PDF pages.' };

export default function Page() {
  return <ToolClient />;
}
