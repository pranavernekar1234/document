import ToolClient from './ToolClient';

export const metadata = { title: 'JPG to PDF', description: 'Convert JPG, PNG, or WebP images into a PDF document instantly.' };

export default function Page() {
  return <ToolClient />;
}
