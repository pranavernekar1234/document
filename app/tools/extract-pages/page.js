import ToolClient from './ToolClient';

export const metadata = { title: 'Extract Pages', description: 'Extract specific pages from a PDF into a new document.' };

export default function Page() {
  return <ToolClient />;
}
