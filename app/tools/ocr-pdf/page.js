import ToolClient from './ToolClient';

export const metadata = { title: 'OCR PDF', description: 'Make scanned PDFs searchable with optical character recognition.' };

export default function Page() {
  return <ToolClient />;
}
