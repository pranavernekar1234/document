import ToolClient from './ToolClient';

export const metadata = { title: 'Compress PDF', description: 'Reduce PDF file size with three compression levels while preserving quality.' };

export default function Page() {
  return <ToolClient />;
}
