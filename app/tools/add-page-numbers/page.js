import ToolClient from './ToolClient';

export const metadata = { title: 'Add Page Numbers', description: 'Insert page numbers to any PDF with customizable position, style, and starting number.' };

export default function Page() {
  return <ToolClient />;
}
