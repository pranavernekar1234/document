import ToolClient from './ToolClient';

export const metadata = { title: 'Remove Pages', description: 'Delete specific pages from a PDF and download the cleaned document.' };

export default function Page() {
  return <ToolClient />;
}
