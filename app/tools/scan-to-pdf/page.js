import ToolClient from './ToolClient';

export const metadata = { title: 'Scan to PDF', description: 'Convert scanned images (JPG, PNG) into a professional PDF document.' };

export default function Page() {
  return <ToolClient />;
}
