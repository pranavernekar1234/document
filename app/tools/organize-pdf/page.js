import ToolClient from './ToolClient';

export const metadata = { title: 'Organize PDF', description: 'Rearrange pages by specifying a new page order.' };

export default function Page() {
  return <ToolClient />;
}
