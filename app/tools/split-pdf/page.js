import ToolClient from './ToolClient';

export const metadata = { title: 'Split PDF', description: 'Split a PDF into multiple files by page ranges or extract every page.' };

export default function Page() {
  return <ToolClient />;
}
