import ToolClient from './ToolClient';

export const metadata = { title: 'PowerPoint to PDF', description: 'Convert PPT and PPTX presentations to PDF with all slides, fonts, and transitions preserved.' };

export default function Page() {
  return <ToolClient />;
}
