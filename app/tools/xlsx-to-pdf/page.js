import ToolClient from './ToolClient';

export const metadata = { title: 'Excel to PDF', description: 'Convert XLS and XLSX spreadsheets to PDF with tables and formatting intact.' };

export default function Page() {
  return <ToolClient />;
}
