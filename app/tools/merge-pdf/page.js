import ToolClient from './ToolClient';

export const metadata = { title: 'Merge PDF', description: 'Combine multiple PDF files into one document in seconds.' };

export default function Page() {
  return <ToolClient />;
}
